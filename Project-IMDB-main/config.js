// If you deploy the frontend to Netlify and the backend separately, set the backend URL here.
// Example: window.API_BASE = "https://my-backend.example";
window.API_BASE = ""; // leave empty to use local paths for development
